// ignore_for_file: prefer_const_constructors

import 'package:bk_ticketing/bk_ticketing.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('BkTicketing', () {
    test('can be instantiated', () {
      expect(BkTicketing(), isNotNull);
    });
  });
}
